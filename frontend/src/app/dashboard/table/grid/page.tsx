"use client";

import {LocalTableSelectionGrid} from "@/components/table/LocalTableSelectionGrid";

const TableGridPage = () => {
    return (
        <div className="h-full">
            <LocalTableSelectionGrid/>
        </div>
    );
};

export default TableGridPage;
